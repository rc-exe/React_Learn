export function DebuggingDemo() {
  return (
    <ul style={{ margin: 0, paddingLeft: '1.25rem', fontSize: '0.9rem' }}>
      <li>Read error messages and stack traces.</li>
      <li>Use browser DevTools (Console, Network).</li>
      <li>Use <strong>React DevTools</strong> to inspect components and state.</li>
      <li>Avoid “refresh and pray” — trace the cause.</li>
    </ul>
  )
}
